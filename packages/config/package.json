{
  "name": "@yx-chat/config",
  "version": "0.0.0",
  "license": "MIT",
  "exports": {
    "./*": {
      "require": {
        "types": "./dist/*.d.ts",
        "default": "./dist/*.js"
      },
      "import": {
        "types": "./dist/*.d.mts",
        "default": "./dist/*.mjs"
      } 
    }
  },
  "scripts": {
    "build": "rm -rf dist && tsup ./*.ts --format cjs,esm --dts",
    "lint": "eslint",
    "preinstall": "npx only-allow pnpm"
  },
  "devDependencies": {
  },
  "files": [
    "dist",
    "./package.json"
  ]
}
